<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="HIPER.InitPage">
    <ContentPage.Content>
        <AbsoluteLayout>
            <StackLayout
                VerticalOptions="CenterAndExpand"
                AbsoluteLayout.LayoutBounds="0,0,1,1"
                AbsoluteLayout.LayoutFlags="All"
                BackgroundColor="White"
                Margin="0">

                <Label Text="HIPER" TextColor="{StaticResource hiperPeach}" FontFamily="FUTURA BOOK" FontSize="60"
                    HorizontalOptions="CenterAndExpand" Margin="0,0,0,100" HorizontalTextAlignment="Center"/>

            </StackLayout>

            <StackLayout
                x:Name="aiLayout"
                IsVisible="False"
                AbsoluteLayout.LayoutBounds="0,0,1,1"
                AbsoluteLayout.LayoutFlags="All"
                BackgroundColor="White" Opacity="0.5">

                <ActivityIndicator
                    x:Name="ai"
                    IsRunning="False"
                    HorizontalOptions="CenterAndExpand"
                    VerticalOptions="CenterAndExpand"
                    Color="Black"/>
            </StackLayout>
        </AbsoluteLayout>

    </ContentPage.Content>
</ContentPage>
